<div class="field">
  <div class="control">
    <label class="checkbox">
      <input type="checkbox" name="{{name}}" id="{{name}}" value=1  @if( old('{{name}}', ${{modelvar}}->{{name}}) ) checked  @endif{{required}}>
      {{friendlyname}}{{asterisk}}
    </label>
  </div>
</div>